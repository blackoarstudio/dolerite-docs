# Only needed for building the site locally. GitHub Pages ignores this file and
# builds with its own pinned dependency set.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick"
